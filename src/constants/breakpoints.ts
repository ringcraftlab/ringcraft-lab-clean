/** Interaction model (edit sheet, sticky CTA, density). Layout skeleton uses CSS @1024px. */
export const INTERACTION_BREAKPOINT_PX = 900

export const interactionMediaQuery = `(max-width: ${INTERACTION_BREAKPOINT_PX}px)` as const
